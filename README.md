# Pump-Runtime
Function test in Matlab. Finds out the lowest runtime of four pumps. Returns two arrays, one includes sorted runtime of the pumps, from lowest to highest. The other one tells which pump is what.
